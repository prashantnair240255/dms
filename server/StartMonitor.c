#include "sheader.h"
#include "methods.h"
#include "GlobalData.h"

void StartMonitor(int* fd)
{
	int nIndex=0;
	int nLength = sizeof(fd)/sizeof(int);
	for(nIndex;nIndex<nLength;nIndex++){
		if((fd[nIndex]!=0)&&(fd[nIndex]!=-1)){
			
		}
	}
}
